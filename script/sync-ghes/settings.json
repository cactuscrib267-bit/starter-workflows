scripts/
  main.ts                       // اسکریپت اصلی (منطق GHES + rewrite)
  workflow-check.ts             // checkWorkflows و checkWorkflow
  workflow-rewrite.ts           // rewriteWorkflow و transformها (مثل placeholder step)
  codeowners-types.ts           // TypeScript interfaces
  codeowners-config.json        // تعریف JSON مالکیت مسیرها
  codeowners-generate.ts        // ساختن .github/CODEOWNERS از JSON
  exec.ts                       // همون exec که الان داری
.github/
  CODEOWNERS                    // خروجی نهایی (auto-generated)